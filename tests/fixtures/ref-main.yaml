openapi: "3.0.0"
info:
  title: "Relative Ref Test API"
  version: "1.0.0"
paths:
  /users:
    get:
      summary: "사용자 목록"
      responses:
        "200":
          description: "성공"
          content:
            application/json:
              schema:
                $ref: "./ref-common.yaml#/components/schemas/UserList"
